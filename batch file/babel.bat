# npm install -g babel-cli
# Add @babel/preset-react (https://git.io/JfeDR) to the 'presets' section of your Babel config to enable transformation.
cd\java
cd SchoolApp
npx babel web\*.js --out-dir build\web
npx babel web\components\*.js --out-dir build\web\components



,
	"build": "babel web\components -d --out-dir build"
	/*"build": "babel src -d lib",
    //"test": "echo \"Error: no test specified\" && exit 1",*/
	
	
	With over 20 years of professional experience in software development, having worked in industries including consulting, banking, oil marketing, education, government ministries etc in London and Lagos. I believe I'm up to the task for this role.